global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "node-api"
    static_configs:
      - targets: ["backend:5000"] # Resolves over Docker's app-network

  - job_name: "cadvisor"
    static_configs:
      - targets: ["cadvisor:8080"]